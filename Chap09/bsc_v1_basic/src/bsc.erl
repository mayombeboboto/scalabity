-module(bsc).
%%% ========================================================= %%%
-behavior(application).
%%% ========================================================= %%%
-export([start/2]).
-export([stop/1]).
%%% ========================================================= %%%
%%% =================== API Functions ======================= %%%
%%% ========================================================= %%%
-spec start(atom() | tuple(), list()) -> {ok, pid()}.
start(_StartType, _StartArgs) ->
    bsc_sup:start_link().

stop(_Data) ->
    ok.